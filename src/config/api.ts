/**
 * API Configuration
 * 
 * Direct API calls to Semantic Scholar API
 * Requires CORS to be disabled in browser (see README.md for setup instructions)
 */

// Semantic Scholar API base URL - used for direct API calls
export const SEMANTIC_SCHOLAR_BASE_URL = 'https://api.semanticscholar.org';
